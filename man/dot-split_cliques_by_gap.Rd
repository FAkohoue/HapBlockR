% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CLQD.R
\name{.split_cliques_by_gap}
\alias{.split_cliques_by_gap}
\title{Split cliques at physical gaps, keeping indices and bp positions in lockstep}
\usage{
.split_cliques_by_gap(cliques_idx, cliques_bp, gapdist)
}
\arguments{
\item{cliques_idx}{List of integer vectors: original column indices per
clique (into \code{binvector}/\code{SNPbps} in \code{CLQD()}).}

\item{cliques_bp}{List of numeric vectors: bp positions, same length and
order as the corresponding element of \code{cliques_idx}.}

\item{gapdist}{Numeric. Gap threshold (bp) above which a clique is split.}
}
\value{
Named list with \code{idx} and \code{bp}, each a list of vectors,
  parallel to each other, after splitting.
}
\description{
Splits each clique wherever the gap between consecutive (sorted) member
bp positions exceeds \code{gapdist}. Takes and returns the SNP column
\emph{indices} alongside their bp positions, sorted/split identically, so
that two SNPs sharing the same bp position (co-located multi-allelic
sites, or an indel and a SNP at the same POS) are never conflated -- each
keeps its own distinct column index through the split, unlike an earlier
version that tracked bp values alone and used \code{match()}/\code{setdiff()}
against those values (ambiguous whenever a bp value isn't unique).
}
\keyword{internal}
