#!/bin/sh
# R supplies SHLIB_OPENMP_CXXFLAGS only when the active compiler toolchain
# supports OpenMP. The C++ code has a serial fallback when the flag is empty.

echo "*** Configuring HapBlockR with R's OpenMP toolchain flags ***"
cp src/Makevars.in src/Makevars
echo "    src/Makevars written"
