#!/bin/sh
# Rtools supplies SHLIB_OPENMP_CXXFLAGS only when the active compiler
# toolchain supports OpenMP. The checked-in Makevars.win uses that variable.

echo "*** Configuring HapBlockR with Rtools OpenMP flags ***"
